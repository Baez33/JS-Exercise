// Los comentarios pueden hacer que el código sea legible.
// Este es mi primer código JS.

/*
   Los comentarios pueden hacer que el código sea legible,
   fácil de reutilizar e informativo.
*/

// En el archivo variable.js:
let cadena;
let booleano;
let indefinido;
let nulo;

// En el archivo datatypes.js:
let variableEjemplo;

// Verificar el tipo de datos de cada variable
console.log(typeof cadena);
console.log(typeof booleano);
console.log(typeof indefinido);
console.log(typeof nulo);

// Declarar cuatro variables sin asignar valores
let variable1, variable2, variable3, variable4;

// Declarar cuatro variables con valores asignados
let variable5 = "Hola", variable6 = true, variable7 = 42, variable8 = null;

// Declarar variables para nombre, apellido, estado civil, país y edad en varias líneas
let nombre,
    apellido,
    estadoCivil,
    pais,
    edad;

// Declarar variables para nombre, apellido, estado civil, país y edad en una sola línea
let nombre2, apellido2, estadoCivil2, pais2, edad2;

// Declarar dos variables myAge y yourAge, asignarles valores iniciales e imprimir en la consola del navegador
let myAge = 25;
let yourAge = 30;
console.log(myAge, yourAge);
